<UserControl x:Class="LCD.View.DataTemplateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LCD.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
   <Grid>
       <Grid.RowDefinitions>
           <RowDefinition Height="Auto"/>
           <RowDefinition/>
       </Grid.RowDefinitions>
       <TextBlock Grid.Row="0" Height="20" x:Name="TextBlock"></TextBlock>
       <DataGrid Name="mydata"
                 Grid.Row="1"
                 Background="White">
           <!--<DataGrid.Columns>
            <DataGridTextColumn Header="ID" Width="30" />
            <DataGridTextColumn Header="X"  Width="80" />
            <DataGridTextColumn Header="Y"  Width="80" />
            <DataGridTextColumn Header="Z"  Width="80" />
            <DataGridTextColumn Header="PG" Width="80"/>
        </DataGrid.Columns>-->
       </DataGrid>
    </Grid>
</UserControl>

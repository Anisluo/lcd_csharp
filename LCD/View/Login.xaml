<Window x:Class="LCD.View.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LCD.View"
        ResizeMode="NoResize"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Login" Height="400" Width="500">
    <Window.Resources>
        <Style x:Key="TxtPwd"
               TargetType="{x:Type TextBox}">
            <Setter Property="TextDecorations">
                <Setter.Value>
                    <TextDecorationCollection>
                        <TextDecoration>
                            <TextDecoration.Pen>
                                <Pen Thickness="10"
                                     Brush="Black"
                                     EndLineCap="Round"
                                     StartLineCap="Round"
                                     DashCap="Round">
                                    <Pen.DashStyle>
                                        <DashStyle Dashes="0.0,1.2"
                                                   Offset="0.6" />
                                    </Pen.DashStyle>
                                </Pen>
                            </TextDecoration.Pen>
                            <TextDecoration.Location>
                                <TextDecorationLocation>Strikethrough</TextDecorationLocation>
                            </TextDecoration.Location>
                        </TextDecoration>
                    </TextDecorationCollection>
                </Setter.Value>


            </Setter>
            <Setter Property="Height"
                    Value="30" />
            <Setter Property="Background"
                    Value="#FF484D5E"></Setter>
            <Setter Property="Foreground"
                    Value="Transparent"></Setter>
            <Setter Property="FontSize"
                    Value="20"></Setter>
            <Setter Property="FontFamily"
                    Value="Courier New"></Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Image Source="../Image/Logo.bmp"></Image>
        <Grid Margin="0,160,0,120"  HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <WrapPanel  HorizontalAlignment="Center" VerticalAlignment="Center">
                <Label Content="用户名" Width="50" Foreground="White" Background="Transparent"></Label>
                <ComboBox Width="120" x:Name="UserName" VerticalAlignment="Center">
                    <ComboBoxItem>Admin</ComboBoxItem>
                </ComboBox>
            </WrapPanel>
            <WrapPanel Grid.Row="1" Margin="2" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Label Content="密   码" Foreground="White" Width="50" Background="Transparent"></Label>
                <TextBox Width="120"
                         Background="White"
                         x:Name="PassWord"
                         VerticalAlignment="Center"
                         Style="{StaticResource TxtPwd}" ></TextBox>
            </WrapPanel>

            <WrapPanel Grid.Row="2"  HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button Content="确定"  Background="Transparent" 
                Foreground="White" FontWeight="Bold"
                Width="100"
                Click="OnBnClickedEnsure"
                BorderBrush="White"
                        ></Button>
            </WrapPanel>
        </Grid>
    </Grid>
</Window>

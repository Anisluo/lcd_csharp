<Window x:Class="LCD.View.CustomMessage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LCD.View"
        xmlns:app1="clr-namespace:LCD"
        ResizeMode="NoResize"
        mc:Ignorable="d"
        Title="{Binding [NoticeMsg], Source={x:Static app1:LanguageManager.Instance}}" Height="250" Width="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="10"/>
        </Grid.RowDefinitions>
        <WrapPanel VerticalAlignment="Center" FlowDirection="RightToLeft">
            <Image Source="../Image/head.png"></Image>
            <Label Content="{Binding [NoticeMsg2], Source={x:Static app1:LanguageManager.Instance}}"  FontSize="20" Margin="0,0,70,0" VerticalAlignment="Center"></Label>
        </WrapPanel>
        <TextBlock Grid.Row="1" HorizontalAlignment="Center" 
                   VerticalAlignment="Center" 
                   Height="30" Margin="20" Name="mymessage">
        </TextBlock>

        <WrapPanel Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="{Binding [OK], Source={x:Static app1:LanguageManager.Instance}}" Click="OnBnClickedEnsure" Background="White" Width="120" ></Button>
            <Button Content="{Binding [Cancel], Source={x:Static app1:LanguageManager.Instance}}" Margin="50,0,0,0" 
            Click="OnBnClickedCancel"
                    Background="White"  Width="120"></Button>
        </WrapPanel>
    </Grid>
</Window>

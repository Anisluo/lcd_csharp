<UserControl x:Class="LCD.View.V110"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LCD.View"
             xmlns:app1="clr-namespace:LCD"
             mc:Ignorable="d"
             xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
             xmlns:airspace="clr-namespace:Microsoft.DwayneNeed.Interop;assembly=Microsoft.DwayneNeed"
             d:DesignHeight="500" d:DesignWidth="660">
    <Grid>
        <!--使用pictureBox进行界面的显示-->

        <airspace:AirspaceDecorator AirspaceMode="Redirect"
                                    Name="myCam"
                                    IsInputRedirectionEnabled="True"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch"
                                    IsOutputRedirectionEnabled="True">
            <WindowsFormsHost   Name="pictureHost"
                                Background="Transparent"
                                Visibility="Visible"
                                Height="500"
                                Width="660"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch">
                <wf:PictureBox x:Name="PictureBoxDisplay"></wf:PictureBox>
            </WindowsFormsHost>
        </airspace:AirspaceDecorator>

        <WindowsFormsHost Background="Green"
                          Height="200"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Width="2">
            <ElementHost>
                <Border Background="Green"></Border>
            </ElementHost>
        </WindowsFormsHost>
        <WindowsFormsHost Background="Green"
                          Height="2"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          Width="200">
            <ElementHost>
                <Border Background="Green"></Border>
            </ElementHost>
        </WindowsFormsHost>
    </Grid>
</UserControl>

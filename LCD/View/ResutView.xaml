<UserControl x:Class="LCD.View.ResutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LCD"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="450" d:DesignWidth="800" DataContext="{Binding Mode=OneWay, RelativeSource={RelativeSource Self}}">
    <!--<DataGrid x:Name="mydata" IsReadOnly="True" ItemsSource="{Binding Datas}" Background="White">
        
    </DataGrid>-->
    <!--<ListView x:Name="mydata" ItemsSource="{Binding Datas}" Background="White">

        <ListView.View>
            <GridView>
                <GridViewColumn Header="测试项" DisplayMemberBinding="{Binding TestName}" Width="150"/>
                <GridViewColumn Header="Num" DisplayMemberBinding="{Binding ID}" Width="50"/>
                <GridViewColumn Header="L" DisplayMemberBinding="{Binding Y,StringFormat=F4}" Width="{Binding DataVisibility.YWidth,Source={x:Static local:Project.cfg},Mode=TwoWay}" />
                <GridViewColumn Header="X" DisplayMemberBinding="{Binding X,StringFormat=F4}" Width="{Binding DataVisibility.XWidth,Source={x:Static local:Project.cfg},Mode=TwoWay}" />
                <GridViewColumn Header="Y" DisplayMemberBinding="{Binding Y,StringFormat=F4}" Width="{Binding DataVisibility.YWidth,Source={x:Static local:Project.cfg},Mode=TwoWay}" />
                <GridViewColumn Header="Z" DisplayMemberBinding="{Binding Z,StringFormat=F4}" Width="{Binding DataVisibility.ZWidth,Source={x:Static local:Project.cfg},Mode=TwoWay}" />
                <GridViewColumn Header="Cx" DisplayMemberBinding="{Binding Cx,StringFormat=F4}" Width="{Binding DataVisibility.CxWidth,Source={x:Static local:Project.cfg},Mode=TwoWay}" />
                <GridViewColumn Header="Cy" DisplayMemberBinding="{Binding Cy,StringFormat=F4}" Width="{Binding DataVisibility.CyWidth,Source={x:Static local:Project.cfg},Mode=TwoWay}" />
                <GridViewColumn Header="u" DisplayMemberBinding="{Binding u,StringFormat=F4}" Width="{Binding DataVisibility.uWidth,Source={x:Static local:Project.cfg},Mode=TwoWay}" />
                <GridViewColumn Header="v" DisplayMemberBinding="{Binding v,StringFormat=F4}" Width="{Binding DataVisibility.vWidth,Source={x:Static local:Project.cfg},Mode=TwoWay}" />
                <GridViewColumn Header="CCT" DisplayMemberBinding="{Binding CCT,StringFormat=F1}" Width="{Binding DataVisibility.CCTWidth,Source={x:Static local:Project.cfg},Mode=TwoWay}" />
                <GridViewColumn Header="完成时间" DisplayMemberBinding="{Binding Time,StringFormat='{}{0:HH:mm:ss:fff}'}" />
            </GridView>
        </ListView.View>
    </ListView>-->

    <Grid>
        <DataGrid Name="ResultData"
                  CanUserAddRows="False"
                  ColumnWidth="Auto"
                  ClipboardCopyMode="ExcludeHeader"
                  SelectionUnit="Cell">
            <!--<ColumnDefinition Width="500"/>-->
            <DataGrid.ContextMenu>
                <ContextMenu  Name="CMCaozuo">
                    <MenuItem Name="CopyValue" Header="复制" FontSize="20" Click="CopyVal_Click" />
                    <!--<Separator/>
                    <MenuItem Name="CopyRow" Header="行拷贝" FontSize="20" Click="CopyRow_Click" />
                    <Separator/>
                    <MenuItem Name="CopyCol" Header="列拷贝" FontSize="20" Click="CopyCol_Click"/>-->
                </ContextMenu>
            </DataGrid.ContextMenu>
        </DataGrid>
    </Grid>
</UserControl>
